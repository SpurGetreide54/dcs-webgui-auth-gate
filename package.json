{
  "name": "dcs-webgui-auth-gate",
  "version": "1.0.0-dev",
  "private": true,
  "license": "GPL-3.0-or-later",
  "description": "Per-admin, per-server login gate and reverse proxy in front of the DCS webgui control panel, plus a mission-upload agent for the game server host.",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "agent": "node src/agent.js",
    "test": "node --test"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^13.0.3",
    "cookie-parser": "^1.4.6",
    "express": "^4.21.0",
    "http-proxy-middleware": "^3.0.0",
    "multer": "^2.2.0",
    "tar": "^7.5.22"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "postject": "^1.0.0-alpha.6"
  }
}
